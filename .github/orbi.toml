# Repository delivery policy (config-as-code, orbi-build/orbi#527).
# Whitelisted policy keys only — everything else (identity, credentials,
# scheduling such as max_concurrency) is host-only and blocks the claim.
# No key is declared today: since orbi-build/orbi#805 the test authority
# is the GitHub Actions CI (merge gate) and the agent's own inference —
# a declared test command would only be a stale hint, so the key was
# removed.
